package xyz.haroldgao.sugarisa.execute;

/**
 * Represents a flag, with the following values: Negative (N), Zero (Z), Carry (C) and Overflow (V).
 * */
public enum ALUFlag {
    N,
    Z,
    C,
    V
}